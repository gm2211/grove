# Formula

This repo doubles as its own Homebrew tap — `grove.rb` here is kept up to date by goreleaser on every release, so `brew tap gm2211/grove https://github.com/gm2211/grove && brew install gm2211/grove/grove` works with no separate tap repo or personal access token.
